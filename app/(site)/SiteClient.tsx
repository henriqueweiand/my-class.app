"use client";

import Container from "../components/Container";

interface SiteClientProps { }

const SiteClient: React.FC<SiteClientProps> = ({ }) => {

  return (
    <Container>
      teste
    </Container>
  );
};

export default SiteClient;
